using System.Net.Mail;

namespace Visitapp.Application.Common.Utils
{
    public static class EmailUtils
    {
        public static bool IsValidEmail(string email)
        {
            try
            {
                var addr = new MailAddress(email);
                return addr.Address == email;
            }
            catch
            {
                return false;
            }
        }
    }
}
